/**
 * `@theographic/core/node` — the Node-only additions to the engine. The
 * browser entry (`@theographic/core`) never imports this file.
 */
export { fsSource } from './fsSource.js';
export * from '../src/index.js';
