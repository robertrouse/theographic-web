{
  "name": "theographic",
  "private": true,
  "description": "Theographic: a knowledge graph of the Bible — static site, in-browser search, native shells",
  "author": "Robert Rouse <robert@viz.bible>",
  "license": "GPL-3.0-or-later",
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "npm run build -w @theographic/core && npm run build -w @theographic/data && npm run build -w @theographic/web",
    "data": "npm run build -w @theographic/core && npm run data -w @theographic/data",
    "dev": "npm run dev -w @theographic/web",
    "preview": "npm run preview -w @theographic/web",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -b",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@types/node": "^22.18.0",
    "prettier": "^3.9.6",
    "typescript": "^5.9.3",
    "vitest": "^5.0.1"
  }
}
