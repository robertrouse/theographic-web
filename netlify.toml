[build]
  base = "/"
  command = "npm run data && npm run build"
  publish = "apps/web/dist"

[build.environment]
  NODE_VERSION = "22"

# Old-URL redirects are generated into apps/web/public/_redirects by CP-06.
